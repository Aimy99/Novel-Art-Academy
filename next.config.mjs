/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', "images.inc.com", "images.pexels.com" ]
    }
};

export default nextConfig;
